import React, { FC, memo } from 'react';
import TeachersTemplate from '../components/templates/TeachersTemplate';

const TeachersPage: FC = memo(() => (
    <TeachersTemplate />
));

export default TeachersPage;
